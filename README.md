# PortScanner

This project is a lightweight TCP port scanner written in Python.
It accepts a domain name or IP address, resolves it using DNS, and scans a list of commonly used service ports to check whether they are open or closed.

The goal of this project was to better understand how network services listen on ports and how connectivity checks work at a low level before relying on automated tools.
